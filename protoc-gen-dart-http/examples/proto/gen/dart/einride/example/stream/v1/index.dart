// Code generated by protoc-gen-dart-http. DO NOT EDIT.

// ignore_for_file: unused_import, unnecessary_cast, avoid_print, non_constant_identifier_names, avoid_equals_and_hash_code_on_mutable_classes

import 'dart:async';
import 'dart:convert';
import '../../../../transport.dart';

const defaultHost = 'api.example.com';

/// A log entry.
class LogEntry {
  /// When the log entry was created.
  String? createTime;
  /// The log message text.
  ///
  /// Behaviors: REQUIRED
  String? message;
  /// The resource name of the log entry.
  String? name;
  /// The severity level.
  String? severity;

  LogEntry({
    this.createTime,
    this.message,
    this.name,
    this.severity,
  });

  factory LogEntry.fromJson(Map<String, dynamic> json) {
    return LogEntry(
      createTime: json['createTime'] as String?,
      message: json['message'] as String?,
      name: json['name'] as String?,
      severity: json['severity'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (createTime != null) json['createTime'] = createTime;
    if (message != null) json['message'] = message;
    if (name != null) json['name'] = name;
    if (severity != null) json['severity'] = severity;
    return json;
  }

  @override
  String toString() {
    return 'LogEntry(createTime: $createTime, message: $message, name: $name, severity: $severity)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is LogEntry &&
      runtimeType == other.runtimeType
      && createTime == other.createTime
      && message == other.message
      && name == other.name
      && severity == other.severity
    ;

  @override
  int get hashCode => Object.hashAll([
    createTime,
    message,
    name,
    severity,
  ]);

  LogEntry copyWith({
    String? createTime,
    String? message,
    String? name,
    String? severity,
  }) {
    return LogEntry(
      createTime: createTime ?? this.createTime,
      message: message ?? this.message,
      name: name ?? this.name,
      severity: severity ?? this.severity,
    );
  }
}

/// Well-known type: Timestamp
///
/// Maps to Dart: `String` (RFC 3339, e.g. `"2021-01-01T00:00:00Z"`).

/// Request message for GetLog.
class GetLogRequest {
  /// The resource name of the log to retrieve.
  ///
  /// Behaviors: REQUIRED
  String? name;

  GetLogRequest({
    this.name,
  });

  factory GetLogRequest.fromJson(Map<String, dynamic> json) {
    return GetLogRequest(
      name: json['name'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (name != null) json['name'] = name;
    return json;
  }

  @override
  String toString() {
    return 'GetLogRequest(name: $name)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is GetLogRequest &&
      runtimeType == other.runtimeType
      && name == other.name
    ;

  @override
  int get hashCode => Object.hashAll([
    name,
  ]);

  GetLogRequest copyWith({
    String? name,
  }) {
    return GetLogRequest(
      name: name ?? this.name,
    );
  }
}

/// Request message for ListLogs.
class ListLogsRequest {
  /// Requested page size.
  int? pageSize;
  /// A token identifying a page of results.
  String? pageToken;

  ListLogsRequest({
    this.pageSize,
    this.pageToken,
  });

  factory ListLogsRequest.fromJson(Map<String, dynamic> json) {
    return ListLogsRequest(
      pageSize: json['pageSize'] as int?,
      pageToken: json['pageToken'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (pageSize != null) json['pageSize'] = pageSize;
    if (pageToken != null) json['pageToken'] = pageToken;
    return json;
  }

  @override
  String toString() {
    return 'ListLogsRequest(pageSize: $pageSize, pageToken: $pageToken)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is ListLogsRequest &&
      runtimeType == other.runtimeType
      && pageSize == other.pageSize
      && pageToken == other.pageToken
    ;

  @override
  int get hashCode => Object.hashAll([
    pageSize,
    pageToken,
  ]);

  ListLogsRequest copyWith({
    int? pageSize,
    String? pageToken,
  }) {
    return ListLogsRequest(
      pageSize: pageSize ?? this.pageSize,
      pageToken: pageToken ?? this.pageToken,
    );
  }
}

/// Response message for ListLogs.
class ListLogsResponse {
  /// The list of log entries.
  List<LogEntry>? logs;
  /// A token to retrieve the next page of results.
  String? nextPageToken;

  ListLogsResponse({
    this.logs,
    this.nextPageToken,
  });

  factory ListLogsResponse.fromJson(Map<String, dynamic> json) {
    return ListLogsResponse(
      logs: (json['logs'] as List<dynamic>?)?.map((e) => LogEntry.fromJson(e as Map<String, dynamic>)).toList(),
      nextPageToken: json['nextPageToken'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (logs != null) json['logs'] = logs!.map((e) => e.toJson()).toList();
    if (nextPageToken != null) json['nextPageToken'] = nextPageToken;
    return json;
  }

  @override
  String toString() {
    return 'ListLogsResponse(logs: $logs, nextPageToken: $nextPageToken)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is ListLogsResponse &&
      runtimeType == other.runtimeType
      && logs == other.logs
      && nextPageToken == other.nextPageToken
    ;

  @override
  int get hashCode => Object.hashAll([
    logs,
    nextPageToken,
  ]);

  ListLogsResponse copyWith({
    List<LogEntry>? logs,
    String? nextPageToken,
  }) {
    return ListLogsResponse(
      logs: logs ?? this.logs,
      nextPageToken: nextPageToken ?? this.nextPageToken,
    );
  }
}

/// Request message for TailLogs.
class TailLogsRequest {
  /// Filter expression for log entries.
  String? filter;
  /// The resource name of the log to tail.
  ///
  /// Behaviors: REQUIRED
  String? name;

  TailLogsRequest({
    this.filter,
    this.name,
  });

  factory TailLogsRequest.fromJson(Map<String, dynamic> json) {
    return TailLogsRequest(
      filter: json['filter'] as String?,
      name: json['name'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (filter != null) json['filter'] = filter;
    if (name != null) json['name'] = name;
    return json;
  }

  @override
  String toString() {
    return 'TailLogsRequest(filter: $filter, name: $name)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is TailLogsRequest &&
      runtimeType == other.runtimeType
      && filter == other.filter
      && name == other.name
    ;

  @override
  int get hashCode => Object.hashAll([
    filter,
    name,
  ]);

  TailLogsRequest copyWith({
    String? filter,
    String? name,
  }) {
    return TailLogsRequest(
      filter: filter ?? this.filter,
      name: name ?? this.name,
    );
  }
}

/// A chat message.
class ChatMessage {
  /// The display name of the sender.
  String? from;
  /// When the message was sent.
  String? sendTime;
  /// The message text.
  ///
  /// Behaviors: REQUIRED
  String? text;

  ChatMessage({
    this.from,
    this.sendTime,
    this.text,
  });

  factory ChatMessage.fromJson(Map<String, dynamic> json) {
    return ChatMessage(
      from: json['from'] as String?,
      sendTime: json['sendTime'] as String?,
      text: json['text'] as String?,
    );
  }

  Map<String, dynamic> toJson() {
    final json = <String, dynamic>{};
    if (from != null) json['from'] = from;
    if (sendTime != null) json['sendTime'] = sendTime;
    if (text != null) json['text'] = text;
    return json;
  }

  @override
  String toString() {
    return 'ChatMessage(from: $from, sendTime: $sendTime, text: $text)';
  }

  @override
  bool operator ==(Object other) =>
    identical(this, other) ||
    other is ChatMessage &&
      runtimeType == other.runtimeType
      && from == other.from
      && sendTime == other.sendTime
      && text == other.text
    ;

  @override
  int get hashCode => Object.hashAll([
    from,
    sendTime,
    text,
  ]);

  ChatMessage copyWith({
    String? from,
    String? sendTime,
    String? text,
  }) {
    return ChatMessage(
      from: from ?? this.from,
      sendTime: sendTime ?? this.sendTime,
      text: text ?? this.text,
    );
  }
}

/// This API demonstrates streaming RPC patterns with HTTP annotations.
/// 
/// - Unary RPCs use the standard ClientTransport (package:http / dio).
/// - Server-streaming RPCs are mapped to SSE (Server-Sent Events).
/// - Bidirectional streaming RPCs are mapped to WebSocket.
class StreamServiceClient {
  final ClientTransport _transport;

  StreamServiceClient(this._transport);

  /// Get a single log entry.
  /// 
  /// Unary request/response — uses ClientTransport.
  Future<LogEntry> getLog(GetLogRequest request, {Map<String, String>? headers}) async {
    if (request.name == null) {
      throw ArgumentError('missing required field request.name');
    }
    final path = 'v1/${request.name}';
    final result = await _transport.unary(path, 'GET', null, TransportMeta(
      service: 'StreamService',
      method: 'GetLog',
    ), headers: headers);
    return LogEntry.fromJson(result as Map<String, dynamic>);
  }

  /// List log entries.
  /// 
  /// Unary request/response with pagination — uses ClientTransport.
  Future<ListLogsResponse> listLogs(ListLogsRequest request, {Map<String, String>? headers}) async {
    final path = 'v1/logs';
    final queryParams = <String>[];
    if (request.pageSize != null) {
      queryParams.add('pageSize=${Uri.encodeComponent(request.pageSize!.toString())}');
    }
    if (request.pageToken != null) {
      queryParams.add('pageToken=${Uri.encodeComponent(request.pageToken!.toString())}');
    }
    var uri = path;
    if (queryParams.isNotEmpty) {
      uri += '?${queryParams.join("&")}';
    }
    final result = await _transport.unary(uri, 'GET', null, TransportMeta(
      service: 'StreamService',
      method: 'ListLogs',
    ), headers: headers);
    return ListLogsResponse.fromJson(result as Map<String, dynamic>);
  }

  /// Tail log entries in real time.
  /// 
  /// Server-streaming RPC → generated as SSE (Server-Sent Events).
  /// The client subscribes to a stream of log entries pushed by the server.
  Stream<LogEntry> tailLogs(TailLogsRequest request, {Map<String, String>? headers}) {
    if (request.name == null) {
      throw ArgumentError('missing required field request.name');
    }
    final path = 'v1/${request.name}:tail';
    final queryParams = <String>[];
    if (request.filter != null) {
      queryParams.add('filter=${Uri.encodeComponent(request.filter!.toString())}');
    }
    var uri = path;
    if (queryParams.isNotEmpty) {
      uri += '?${queryParams.join("&")}';
    }
    return _transport.serverStream(uri, TransportMeta(
      service: 'StreamService',
      method: 'TailLogs',
    ), headers: headers)
      .map((json) => LogEntry.fromJson(json));
  }

  /// Realtime bidirectional chat.
  /// 
  /// Bidirectional streaming RPC → generated as WebSocket.
  /// Both client and server can send messages independently.
  TypedDuplexConnection<ChatMessage, ChatMessage> chat({Map<String, String>? headers}) {
    final path = 'v1/chat';
    return TypedDuplexConnection<ChatMessage, ChatMessage>( 
      _transport.duplexStream(path, TransportMeta(
        service: 'StreamService',
        method: 'Chat',
      ), headers: headers),
      (json) => ChatMessage.fromJson(json),
      (data) => data.toJson(),
    );
  }
}

class ApiClient {
  final ClientTransport _transport;

  StreamServiceClient? _streamService;

  ApiClient(this._transport);

  StreamServiceClient get streamService {
    _streamService ??= StreamServiceClient(_transport);
    return _streamService!;
  }

  /// Closes all service clients and releases resources.
  void dispose() {
    _streamService = null;
  }
}

ApiClient createApiClient(ClientTransport transport) {
  return ApiClient(transport);
}


// @@protoc_insertion_point(dart-http-eof)
