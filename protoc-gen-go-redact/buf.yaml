version: v2

modules:
  - path: .
    lint:
      use:
        - STANDARD
    breaking:
      use:
        - FILE

deps:
  - "buf.build/go-wind/redact"

breaking:
  use:
    - FILE

lint:
  use:
    - DEFAULT
